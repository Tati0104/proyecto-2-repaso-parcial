let a = 5;  // Declaración e inicialización de la variable 'a'
let b = 4;  // Declaración e inicialización de la variable 'b'

a++;        // Incrementa 'a' en 1, por lo tanto, a = 6
b++;        // Incrementa 'b' en 1, por lo tanto, b = 5

a += 5;     // Suma 5 al valor actual de 'a', por lo tanto, a = 11

console.log(a + b);  // Imprime la suma de 'a' y 'b', que es 11 + 5 = 16