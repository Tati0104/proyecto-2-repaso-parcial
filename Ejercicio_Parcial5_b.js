let a = 4;  // Declaración e inicialización de la variable 'a' con el valor 4
let b = 3;  // Declaración e inicialización de la variable 'b' con el valor 3

a = b ** 2; // Eleva 'b' al cuadrado y asigna el resultado a 'a', por lo tanto, a = 9

if (a > b) { // Evalúa si 'a' es mayor que 'b'
    console.log(4); // Como la condición es verdadera, imprime 4 en la consola
}